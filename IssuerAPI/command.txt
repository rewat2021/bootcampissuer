dotnet ef dbcontext scaffold "server=<HOST>;port=3306;database=issuer;user=<USER>;password=<PASSWORD>;sslmode=None" Pomelo.EntityFrameworkCore.MySql -c IssuerDbContext -o Databases --force

dotnet ef dbcontext scaffold "Name=CONNECTION_STRING" Pomelo.EntityFrameworkCore.MySql -o Databases -f --no-onconfiguring


# C-06: real MySQL root credentials were removed from this file (they were committed to source
# control alongside the rest of the codebase). Set CONNECTION_STRING as an environment variable
# instead — see IssuerDbContext.OnConfiguring, which now refuses to start without it.
# optionsBuilder.UseMySql(Environment.GetEnvironmentVariable("CONNECTION_STRING"), Microsoft.EntityFrameworkCore.ServerVersion.Parse("8.0.45-mysql"));
