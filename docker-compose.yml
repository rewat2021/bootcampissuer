version: '3.8'

services:
  issuer-mysql:
    image: mysql:8.0
    container_name: issuer-mysql
    environment:
      # C-06: no default — a hardcoded real-looking password here was committed to source before.
      # Set MYSQL_ROOT_PASSWORD in your .env file (gitignored); compose fails fast if it's unset.
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:?set MYSQL_ROOT_PASSWORD in .env}
      MYSQL_DATABASE: issuer
    volumes:
      - issuer-mysql-data:/var/lib/mysql
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    ports:
      - "3306:3306"
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uroot", "-p${MYSQL_ROOT_PASSWORD:?set MYSQL_ROOT_PASSWORD in .env}"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    restart: unless-stopped
    networks:
      - lab-network

  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: issuer-api
    ports:
      - "5002:8080"
    env_file:
      - .env
    depends_on:
      issuer-mysql:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - lab-network

volumes:
  issuer-mysql-data:

networks:
  lab-network:
    external: true
